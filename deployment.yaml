apiVersion: v1
kind: Namespace
metadata:
  name: goto-2
  namespace: goto-2
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: goto-2
  namespace: goto-2
  labels:
    app: goto-2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: goto-2
  template:
    metadata:
      labels:
        app: goto-2
    spec:
      containers:
        - name: goto-2
          image: codydearkland/ld-demo-goto:2
          imagePullPolicy: Always
          ports:
            - name: web
              containerPort: 3000
              protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  namespace: goto-2
  name: goto-lb
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: goto-2
  type: LoadBalancer